#include<iostream>
using namespace std;

int main(){
    int x=3,y=5;
    if(x==3)
        cout<<"x: "<<x<<endl; // doesn't need any bracket if we code only one line after if-else statements
    else;
        cout<<"y: "<<y<<endl; // doesn't matter that else statement hits or not, this line will be executed.
    
    return 0;
}