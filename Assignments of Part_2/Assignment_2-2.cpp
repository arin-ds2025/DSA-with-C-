// fint the output for this code:

#include <iostream>
using namespace std;

int main(){
    int x,y;
    cout<<"Enter the first number: ";
    cin>>x;
    cout<<"Enter the second number: ";
    cin>>y;
    cout<<"(x != y) is: "<<(x != y)<<" "<<"(x >= y ) is: "<<(x >= y)<<endl;
    return 0;
}