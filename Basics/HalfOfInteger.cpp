#include<iostream>
using namespace std;
int main(){
    int n;
    cout<<"Enter an integer: ";
    cin>>n;
    // float half = float(n) / 2;
    // cout<<"Half of "<<n<<" is "<<half<<endl;

    // in shortcut .. without typecasting
    cout<<"Half of "<<n<<" is "<<n/2.0<<endl; 

    
}