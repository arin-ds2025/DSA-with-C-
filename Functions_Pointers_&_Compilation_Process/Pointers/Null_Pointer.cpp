#include<bits/stdc++.h>
using namespace std;

int main(){
    int x = 758572;
    int* ptr = NULL; // Null Pointer
    cout<<"ptr before initialize the address of x: "<<ptr<<endl;

    //int * ptr2 = '\0'; // '\0' this is a null character ... so this is Null Pointer
    // cout<<"ptr2 before initialize the address of x: "<<ptr2<<endl;
    // cout<<int('\0')<<endl; // it will print 0.. so the ascii value of null character is 0

    // *ptr = &x;
    // cout<<"ptr after initialize the address of x: "<<ptr<<endl;
}
