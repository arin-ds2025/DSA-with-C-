#include<bits/stdc++.h>
using namespace std;

int main () {
    int m = 12;
    while(m/=2) cout<<"Aggeee..!"<<endl; // while the value of m will be 0 then the loop will stop
    // cause 0 means false. m/=2 means reassigning the value in m which is continously dividing by 2
}